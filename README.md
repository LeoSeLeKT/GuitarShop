Features:
-Guitar brands listing
-Models listing with search, filter, and pagination
=Guitar details page with tabs for specifications and musicians
=Styled UI using CSS
=Apollo Client integration

Setup Instructions:
-Run 'npm install'
-Then run 'npm start' to launch the app

On some of the Fender guitars there aren't pictures because the pictures can't be loaded from the API 
I tried accessing the pictures from the API but they are "No access granted"
